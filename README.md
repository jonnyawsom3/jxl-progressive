# jxl-progressive

Progressive rendering demo of JPEG XL using jxl-rs.

## Prerequisites

`ffmpeg` is needed because this demo encodes progression as a video. It should be set up so that
`pkg-config` can find the installation.
